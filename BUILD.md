# Инструкция по сборке проекта Jummah Prayer

## 📋 Требования

### macOS
- macOS 10.15 или новее
- Xcode Command Line Tools
- Homebrew (для установки зависимостей)

### Linux
- Ubuntu 20.04+ / Fedora 34+ / другой современный дистрибутив
- GCC 9+ или Clang 10+
- CMake 3.16+

## 🔧 Установка зависимостей

### macOS

```bash


# Установка зависимостей
brew install cmake qt@6

# Добавление Qt в PATH (добавьте в ~/.zshrc или ~/.bashrc)
export PATH="/opt/homebrew/opt/qt@6/bin:$PATH"
export Qt6_DIR="/opt/homebrew/opt/qt@6/lib/cmake/Qt6"
```

### Linux (Ubuntu/Debian)

```bash
sudo apt update
sudo apt install -y build-essential cmake git

# Qt 6
sudo apt install -y qt6-base-dev qt6-declarative-dev qt6-positioning-dev qt6-sensors-dev
```

### Linux (Fedora)

```bash
sudo dnf install cmake gcc-c++ git
sudo dnf install qt6-qtbase-devel qt6-qtdeclarative-devel qt6-qtpositioning-devel qt6-qtsensors-devel
```

## 🏗️ Сборка проекта

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd jummah-prayer
```

### Шаг 2: Создание директории для сборки

```bash
mkdir build
cd build
```

### Шаг 3: Конфигурация проекта

```bash
cmake ..
```

**По умолчанию:** Локальная сборка для текущей архитектуры (быстрее для разработки)

**Опции конфигурации:**
```bash
# Отключить тесты
cmake -DBUILD_TESTS=OFF ..

# Указать путь к Qt вручную (если не найден автоматически)
cmake -DQt6_DIR=/path/to/qt6/lib/cmake/Qt6 ..

# Сборка для Android (требуется Android SDK/NDK)
cmake -DANDROID_ABI=arm64-v8a -DANDROID_PLATFORM=android-28 ..
```

**Сборка для разных архитектур macOS:**
```bash
# Локальная сборка (для текущей архитектуры) - по умолчанию
cmake -DBUILD_UNIVERSAL=OFF ..

# Универсальная сборка (работает на Intel и Apple Silicon)
cmake -DBUILD_UNIVERSAL=ON ..

# Только для Apple Silicon (arm64)
cmake -DBUILD_ARM64_ONLY=ON ..

# Только для Intel Mac (x86_64)
cmake -DBUILD_X86_64_ONLY=ON ..

# Указать минимальную версию macOS
cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 ..
```

### Шаг 4: Сборка

```bash
# Параллельная сборка (рекомендуется)
cmake --build . -j4

# Или использовать make напрямую
make -j4
```

### Шаг 5: Запуск приложения

```bash
# macOS/Linux
./JummahPrayer

# Или из корня проекта
./build/JummahPrayer
```

## 🧪 Запуск тестов

```bash
# Из директории build
ctest --output-on-failure

# Или запустить отдельные тесты
./tests/test_prayertimes
./tests/test_qml_ui
```

## 🐛 Отладка

### Пересборка с нуля

```bash
cd build
rm -rf *
cmake ..
cmake --build . -j4
```

### Просмотр подробных логов сборки

```bash
cmake --build . --verbose
```

### Проверка зависимостей Qt

```bash
# macOS
brew info qt@6

# Linux
dpkg -l | grep qt6  # Debian/Ubuntu
rpm -qa | grep qt6  # Fedora/RHEL
```

## 📱 Сборка для Android

### Требования
- Android SDK (API 28+)
- Android NDK (r21+)
- Qt for Android (6.6+)

### Сборка APK

```bash
# Установка зависимостей
brew install android-sdk android-ndk  # macOS
# или скачайте Android Studio

# Конфигурация
export ANDROID_SDK_ROOT=/path/to/android/sdk
export ANDROID_NDK_ROOT=/path/to/android/ndk

# Сборка
mkdir build-android
cd build-android
cmake -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_ROOT/build/cmake/android.toolchain.cmake \
      -DANDROID_ABI=arm64-v8a \
      -DANDROID_PLATFORM=android-28 \
      -DQt6_DIR=/path/to/qt/android/lib/cmake/Qt6 \
      ..
      
cmake --build .

# Создание APK
androiddeployqt --input android-JummahPrayer-deployment-settings.json \
                --output android-build \
                --android-platform android-28 \
                --gradle
```

## 🚀 Быстрый старт (macOS)

```bash
# Одной командой (локальная сборка)
brew install cmake qt@6 && \
export Qt6_DIR="/opt/homebrew/opt/qt@6/lib/cmake/Qt6" && \
make build && \
make run
```

**Или используя Makefile:**
```bash
# Локальная сборка (быстрее, для разработки)
make build

# Универсальная сборка (для распространения)
make build-universal
```

### Сборка для разных архитектур Mac

**Используя Makefile (рекомендуется):**

Сборки разделены по разным директориям для удобства:

```bash
# Локальная сборка (build/) - быстрее для разработки
make build
make run          # Запуск локальной сборки
make test         # Тесты локальной сборки

# Универсальная сборка (build-universal/) - для распространения
make build-universal
make deploy-universal # Упаковка зависимостей Qt (для распространения!)
make run-universal    # Запуск универсальной сборки
make test-universal   # Тесты универсальной сборки

# Только для Apple Silicon (build-arm64/)
make build-arm64

# Только для Intel Mac (build-x86_64/)
make build-x86_64

# Очистка
make clean          # Очистить локальную сборку
make clean-universal # Очистить универсальные сборки
make clean-all      # Очистить все сборки
```

**Используя CMake напрямую:**
```bash
# Локальная сборка
mkdir build && cd build
cmake ..
cmake --build . -j4

# Универсальная сборка (отдельная директория)
mkdir build-universal && cd build-universal
cmake -DBUILD_UNIVERSAL=ON ..
cmake --build . -j4

# Только для Apple Silicon
mkdir build-arm64 && cd build-arm64
cmake -DBUILD_ARM64_ONLY=ON ..
cmake --build . -j4

# Только для Intel
mkdir build-x86_64 && cd build-x86_64
cmake -DBUILD_X86_64_ONLY=ON ..
cmake --build . -j4
```

**Проверка архитектуры собранного приложения:**
```bash
# Локальная сборка
file build/JummahPrayer.app/Contents/MacOS/JummahPrayer
# Покажет: "Mach-O 64-bit executable arm64" или "x86_64"

# Универсальная сборка
file build-universal/JummahPrayer.app/Contents/MacOS/JummahPrayer
# Должно показать: "Mach-O universal binary with 2 architectures: [x86_64:arm64]"
```

## 📦 Деплой приложения для распространения

Для запуска приложения на других Mac без установленного Qt необходимо упаковать все зависимости:

```bash
# Деплой локальной сборки
make deploy

# Деплой универсальной сборки (рекомендуется для распространения)
make build-universal
make deploy-universal
```

**Что делает деплой:**
- Копирует все необходимые библиотеки Qt в bundle приложения
- Изменяет пути к библиотекам на относительные (@rpath)
- Делает приложение самодостаточным (portable)

**После деплоя:**
- Приложение можно копировать на другие Mac
- Не требуется установка Qt на целевом компьютере
- Все зависимости включены в `JummahPrayer.app`

**Использование macdeployqt напрямую:**
```bash
# Для локальной сборки
macdeployqt build/JummahPrayer.app -always-overwrite

# Для универсальной сборки
macdeployqt build-universal/JummahPrayer.app -always-overwrite
```

## 📦 Структура проекта

```
jummah-prayer/
├── src/                    # C++ исходники
│   ├── main.cpp
│   ├── PrayerTimesCalculator.h/cpp
│   ├── AppSettings.h/cpp
│   └── LocationService.h/cpp
├── qml/                    # QML интерфейс
│   ├── Main.qml
│   ├── MainPage.qml
│   ├── SettingsPage.qml
│   ├── QiblaPage.qml
│   ├── DhikrPage.qml
│   ├── Theme.qml
│   └── Translator.qml
├── tests/                  # Тесты
│   ├── test_prayertimes.cpp
│   └── test_qml_ui.cpp
├── CMakeLists.txt         # Конфигурация сборки
└── README.md
```

## ⚙️ Системные требования

### Минимальные
- CPU: 1 GHz
- RAM: 512 MB
- Диск: 50 MB

### Рекомендуемые
- CPU: 2 GHz dual-core
- RAM: 2 GB
- Диск: 100 MB
- GPS модуль (для автоопределения местоположения)
- Магнитометр (для компаса Киблы)

## 🆘 Решение проблем

### Qt не найден

```bash
# macOS
export Qt6_DIR="/opt/homebrew/opt/qt@6/lib/cmake/Qt6"

# Linux - проверьте установку
which qmake6
```

### Ошибки компиляции

```bash
# Убедитесь что используется правильная версия компилятора
cmake -DCMAKE_CXX_COMPILER=clang++ ..  # macOS
cmake -DCMAKE_CXX_COMPILER=g++ ..      # Linux
```

### Приложение не запускается

```bash
# Проверьте наличие библиотек Qt
ldd ./JummahPrayer  # Linux
otool -L ./JummahPrayer  # macOS

# Очистите настройки
rm -rf ~/.config/JummahPrayer
```

### Ошибка "Library not loaded" на другом Mac

Если приложение не запускается на другом Mac с ошибкой о не найденных библиотеках Qt:

```bash
# Выполните деплой для упаковки всех зависимостей Qt
make deploy          # Для локальной сборки
make deploy-universal # Для универсальной сборки

# После деплоя приложение будет содержать все необходимые библиотеки Qt
# и сможет запускаться на других Mac без установленного Qt
```

**Важно:** После деплоя приложение можно распространять - все зависимости Qt будут включены в bundle.

## 📝 Примечания

- Первая сборка займёт 1-2 минуты
- Последующие сборки: 10-30 секунд
- Для разработки рекомендуется Qt Creator
- Тесты запускаются автоматически при `ctest`

## 🌐 Поддерживаемые платформы

- ✅ macOS (Intel & Apple Silicon) - универсальная сборка поддерживается
- ✅ Linux (x86_64, ARM64)
- ✅ Android (ARM64-v8a)
- ⏳ iOS (планируется)
- ⏳ Windows (планируется)

### Подробности поддержки macOS

Проект поддерживает сборку для разных архитектур macOS:

- **Универсальная сборка (Universal Binary)**: Один бинарник работает на Intel и Apple Silicon Mac
- **Только Apple Silicon (arm64)**: Оптимизированная сборка для M1/M2/M3 Mac
- **Только Intel (x86_64)**: Для старых Intel Mac

По умолчанию используется универсальная сборка, которая работает на всех современных Mac.

## 📄 Лицензия

MIT License - см. файл LICENSE

