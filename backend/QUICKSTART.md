# Быстрый старт C++ Backend

## Сборка и запуск

```bash
# Перейти в директорию бэкенда
cd web-backend

# Создать директорию сборки
mkdir build
cd build

# Собрать проект
cmake ..
cmake --build .

# Запустить сервер
./JummahPrayerBackend
```

Сервер запустится на `http://localhost:8080`

## Использование

1. Откройте в браузере: `http://localhost:8080`
2. Веб-интерфейс автоматически подключится к C++ API
3. Все расчеты времени молитв выполняются на C++ бэкенде

## API Endpoint

### GET /api/prayer-times

**Пример запроса:**
```
http://localhost:8080/api/prayer-times?lat=55.7558&lon=37.6173&city=Москва&method=3&madhhab=0
```

**Параметры:**
- `lat` (обязательно) - широта
- `lon` (обязательно) - долгота  
- `city` (опционально) - название города
- `method` (опционально) - метод расчета (0-5)
- `madhhab` (опционально) - мазхаб (0=Shafi'i, 1=Hanafi)
- `year`, `month`, `day` (опционально) - дата

**Ответ:**
```json
{
  "success": true,
  "data": {
    "fajr": "05:23",
    "sunrise": "07:15",
    "dhuhr": "12:30",
    "asr": "15:45",
    "maghrib": "17:50",
    "isha": "19:20",
    "date": "15.12.2024",
    "city": "Москва",
    "latitude": 55.7558,
    "longitude": 37.6173,
    "currentPrayer": "Dhuhr",
    "nextPrayer": "Asr"
  }
}
```

## Особенности

- ✅ Чистый C++ без Qt
- ✅ Использует вашу логику расчета из `src/PrayerTimesCalculator.cpp`
- ✅ Легковесный HTTP сервер (cpp-httplib)
- ✅ Автоматическая раздача статических файлов из `../web/`

---

**Ассаламу алейкум!** 🌙

