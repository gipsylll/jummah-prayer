# Jummah Prayer - C++ Backend

C++ HTTP сервер для веб-версии приложения.

## 🎯 Назначение

Этот компонент:
- ✅ Предоставляет REST API для расчета времени молитв
- ✅ Раздает статические файлы фронтенда из `../frontend/`
- ✅ Использует вашу C++ логику расчета (без Qt зависимостей)
- ✅ Работает на одном порту (8080) - и API, и фронтенд

## 📁 Структура

```
backend/
├── src/
│   ├── server.cpp              # HTTP сервер (cpp-httplib)
│   ├── PrayerTimesCalculator.h # Калькулятор времени молитв
│   └── PrayerTimesCalculator.cpp
├── CMakeLists.txt
└── README.md
```

## 🔌 API Endpoints

### GET /api/prayer-times

Получить время молитв.

**Параметры:**
- `lat` (обязательно) - широта
- `lon` (обязательно) - долгота
- `city` (опционально) - название города
- `method` (опционально) - метод расчета (0-5)
- `madhhab` (опционально) - мазхаб (0=Shafi'i, 1=Hanafi)
- `year`, `month`, `day` (опционально) - дата

**Пример:**
```
GET /api/prayer-times?lat=55.7558&lon=37.6173&city=Москва&method=3
```

### Статические файлы

Сервер автоматически раздает файлы из `../frontend/`:
- `GET /` → `index.html`
- `GET /styles.css` → `styles.css`
- `GET /app.js` → `app.js`
- и т.д.

## 🚀 Сборка и запуск

```bash
# Сборка
make web-backend-build

# Запуск (интерактивно)
make web-run

# Или с указанием пути к фронтенду
cd backend/build
./JummahPrayerBackend /полный/путь/к/frontend/
```

## 🛠 Технологии

- **cpp-httplib** - легковесная header-only HTTP библиотека
- **C++17** - стандарт языка
- **CMake** - система сборки

## 📝 Особенности

- ✅ Чистый C++ без Qt
- ✅ Использует вашу логику из `mobile/src/PrayerTimesCalculator.cpp` (переписана без Qt)
- ✅ Все расчеты выполняются на сервере
- ✅ CORS поддержка для фронтенда
- ✅ Автоматическая раздача статических файлов

---

**Ассаламу алейкум!** 🌙
