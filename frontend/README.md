# Jummah Prayer - Web Frontend

Веб-интерфейс для приложения, работающий в браузере.

## 🎯 Назначение

Этот компонент:
- ✅ HTML/CSS/JavaScript интерфейс
- ✅ Работает в браузере
- ✅ Обращается к C++ бэкенду через REST API
- ✅ Не требует отдельного веб-сервера (раздается бэкендом)

## 📁 Структура

```
frontend/
├── index.html              # Главная страница
├── styles.css              # Стили приложения
├── app.js                  # Главная логика
├── prayer-calculator.js    # Работа с C++ API
├── translations.js        # Система переводов (RU, EN, AR)
└── manifest.json          # PWA манифест
```

## 🔌 Работа с API

Фронтенд обращается к C++ бэкенду:

```javascript
// Пример запроса
const response = await fetch('/api/prayer-times?lat=55.7558&lon=37.6173&method=3');
const data = await response.json();
```

**Все расчеты выполняются на C++ бэкенде!**

## 🚀 Запуск

Фронтенд раздается автоматически C++ бэкендом:

```bash
make web-start    # Запускает бэкенд + открывает фронтенд
```

Затем откройте: `http://localhost:8080`

## ✨ Функции

- ✅ Расчет времени молитв (через C++ API)
- ✅ Автоматическое определение местоположения
- ✅ Поиск городов
- ✅ Календарь для выбора даты
- ✅ Обратный отсчет до следующей молитвы
- ✅ Темная тема
- ✅ Многоязычность (Русский, English, العربية)
- ✅ Счетчик зикра
- ✅ Настройки приложения
- ✅ PWA поддержка (можно установить как приложение)

## 🛠 Технологии

- **HTML5** - структура
- **CSS3** - стили (с CSS переменными для тем)
- **Vanilla JavaScript** - логика (без фреймворков)
- **PWA** - поддержка установки как приложение

## 📝 Особенности

- ✅ Не требует сборки (просто HTML/CSS/JS)
- ✅ Работает через C++ бэкенд
- ✅ Все расчеты на сервере
- ✅ Адаптивный дизайн
- ✅ Работает офлайн (после первой загрузки)

---

**Ассаламу алейкум!** 🌙
