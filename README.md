# Jummah Prayer

Приложение для расчёта времени намаза, разработанное на Qt 6 (C++ и QML).

## 📦 Компоненты проекта

Проект состоит из **трех независимых компонентов**:

1. **📱 Нативное приложение** (Qt/QML) - для Desktop и Android(не собрал)
2. **🌐 Веб-версия** - C++ Backend + Frontend (работает в браузере)
3. **🔄 Общая логика** - расчет времени молитв на C++

Подробнее см. [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)

---

## 🚀 Быстрый старт

### Мобильное приложение (mobile/)

**Требования:**
- Qt 6.5+ (рекомендуется Qt 6.7)
- CMake 3.16+
- C++17 компилятор

**Сборка:**
```bash
make build
make run
```

Подробнее: см. [BUILD.md](BUILD.md)

### Веб-версия (backend/ + frontend/)

**Требования:**
- CMake 3.16+
- C++17 компилятор
- Git (для загрузки cpp-httplib)

**Запуск:**
```bash
make web-start    # Запускает бэкенд + открывает фронтенд в браузере
```

Подробнее: см. `backend/README.md` и `frontend/README.md`

---

## 📁 Структура проекта

Проект разделен на **3 независимые папки**:

```
jummah-prayer/
├── 📱 mobile/              # Мобильное приложение (Qt/QML)
│   ├── src/                # C++ логика
│   ├── qml/                # QML интерфейс
│   └── CMakeLists.txt
├── 🔧 backend/             # C++ HTTP сервер (бэкенд)
│   ├── src/
│   │   ├── server.cpp      # HTTP сервер
│   │   └── PrayerTimesCalculator.*  # Логика (без Qt)
│   └── CMakeLists.txt
├── 🌐 frontend/            # Веб-интерфейс (Frontend)
│   ├── index.html
│   ├── styles.css
│   └── *.js
├── Makefile                # Команды для всех компонентов
└── PROJECT_STRUCTURE.md    # Подробная структура
```

---

## 📋 Текущий статус

✅ **Фаза 0**: Минимальный рабочий пример  
✅ **Фаза 1**: Расчёт времён намаза и геолокация  
✅ **Фаза 2**: Уведомления и компас  
⬜ **Фаза 3**: Расширенный функционал  

Смотрите [roadmap.md](roadmap.md) для детального плана разработки.

---

## 🛠 Команды Makefile

### Мобильное приложение (mobile/):
- `make build` - собрать
- `make run` - запустить
- `make deploy` - упаковать для распространения

### Веб-версия (backend/ + frontend/):
- `make web-start` - запустить бэкенд + фронтенд
- `make web-run` - запустить только бэкенд (интерактивно)
- `make web-stop` - остановить бэкенд
- `make web-backend-build` - собрать бэкенд

### Общие:
- `make clean` - очистить сборку
- `make help` - справка по всем командам

---

## 👥 Команда разработки

- **Алена Марчук** - Frontend (React), веб-интерфейс, UI/UX, Notifications
- **Пасичниченко Глеб** - Backend (C++ HTTP сервер), база данных, геолокация, интеграция с фронтом
- **Павлов Иван** - Мобильное приложение (Qt/QML), основные QML-страницы, тесты

---

## 📄 Лицензия

Этот проект разрабатывается с использованием Qt (LGPL/Commercial License).

---

**Ассаламу алейкум!** 🌙
