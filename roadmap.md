Конечно! Ниже — **финальный, уточнённый roadmap и функциональные требования** для приложения **только на Qt (C++)**, **без iOS**, **без веб-версии**, **только Android**.

> ⚠️ **Важно**: поскольку ты выбрал **Qt**, мы **отказываемся от веб-версии** (она технически невозможна в полноценном виде).  
> Если позже понадобится сайт — его придётся делать отдельно (на другом стеке).

---

# 🧭 Roadmap: Приложение для Намаза на Qt (C++ / Android Only)

## **Фаза 0: Подготовка и настройка окружения (1–2 недели)**

### 🎯 Цель:
Готовая среда разработки и минимальный рабочий проект Qt для Android.

### ✅ Задачи:
- Установить **Qt 6.7+** (или 6.5 LTS) с поддержкой Android.
- Настроить **Android SDK, NDK, JDK** в Qt Creator.
- Создать **минимальный QML-проект**, собрать APK, запустить на устройстве.
- Подключить **C++ логику** к QML (через `Q_INVOKABLE` или `Q_PROPERTY`).
- Инициализировать репозиторий с понятной структурой.

---

## **Фаза 1: Ядро + базовый UI (4–6 недель)**

### 🎯 Цель:
Приложение показывает точное время намаза для текущей геолокации.

### 🔧 Технологии:
- **C++**: расчёт времён намаза (чистые функции).
- **QML**: UI (список времён, заголовок, настройки).
- **Qt Sensors**: компас (позже).
- **Qt Positioning**: геолокация.

### 📋 Функциональные требования (MVP):

| ID | Фича | Описание |
|----|------|--------|
| F1 | Расчёт времён намаза | C++ функция: по координатам, дате, методу, мазхабу возвращает 5 времён. |
| F2 | Автоматическая геолокация | Использовать `QGeoPositionInfoSource` для получения широты/долготы. |
| F3 | Ручной ввод координат / выбор города | Временно — через поле ввода (позже — база городов). |
| F4 | Выбор метода расчёта | Выпадающий список: MWL, ISNA, Egypt, Karachi, Umm al-Qura, Tehran. |
| F5 | Переключатель мазхаба | Флаг "Ханафи" (влияет на Аср). |
| F6 | Отображение времён | Чёткий список: Фаджр, Зухр, Аср, Магриб, Иша + текущий намаз. |
| F7 | Сохранение настроек | Через `QSettings` (сохраняется между запусками). |
| F8 | Поддержка темной темы | Авто или ручная (через QML Theme). |

---

## **Фаза 2: Системные интеграции (4–6 недель)**

### 🎯 Цель:
Приложение работает как полноценный помощник: с уведомлениями, компасом, оффлайн.

### 📋 Новые функциональные требования:

| ID | Фича | Описание |
|----|------|--------|
| F9 | Локальные уведомления | Напоминание за 5–10 мин до намаза (через нативный Android-код + `QAndroidJniObject`). |
| F10 | Компас Кыблы | Использовать `QCompass` из Qt Sensors. Визуальное указание направления. |
| F11 | Оффлайн-режим | После первого запуска — всё работает без интернета (геолокация требует GPS, но не сеть). |
| F12 | Виджет на главном экране | Android App Widget (требует нативного Java-кода). |
| F13 | Вибрация при уведомлении | Через Android API (`Vibrator`). |

> ⚠️ Уведомления и виджеты **требуют написания небольшого Java-кода** — это неизбежно в Qt для Android.

---

## **Фаза 3: Духовный и расширенный функционал (6+ недель)**

### 📋 Фичи:

| ID | Фича | Описание |
|----|------|--------|
| F14 | Календарь на месяц | QML `TableView` или `Repeater` с временами на 30 дней. |
| F15 | Рамадан-режим | Автоопределение Рамадана, таймер до ифтара, специальный UI. |
| F16 | Зикры и дуа | Список с возможностью отметки "прочитано" (локальное состояние). |
| F17 | Аудио-азан | Воспроизведение `.mp3` при наступлении времени (через `QMediaPlayer`). |
| F18 | База городов | Встроенный JSON/SQLite со списком городов и координатами. |
| F19 | Мультиязычность | Поддержка русского, английского, арабского, урду (через Qt Linguist). |
| F20 | Экспорт в PDF | Генерация PDF с расписанием (через `QPrinter` + `QPdfWriter`). |

---

## **Фаза 4: Публикация и поддержка**

- Тестирование на разных устройствах Android.
- Оптимизация размера APK (минификация, раздельные сборки по архитектуре).
- Публикация в **Google Play**.
- Сбор отзывов, обновления.

---

# 📐 Нефункциональные требования

| Категория | Требование |
|----------|-----------|
| **Язык** | C++ (Qt 6), QML, минимальный Java (для уведомлений) |
| **Платформа** | Android 8.0+ (API 26) |
| **Точность** | Времена намаза — погрешность ≤ 1–2 минут |
| **Размер APK** | ≤ 35 МБ (после оптимизации) |
| **Оффлайн** | Полная работоспособность без интернета (кроме первоначального получения координат через GPS) |
| **Производительность** | Запуск < 1.5 сек, расчёт < 10 мс |
| **Безопасность** | Никаких сетевых запросов без согласия пользователя |
| **Лицензия** | LGPL (открытая) или коммерческая Qt |

---

# 🛠 Архитектура проекта

```
namaz-qt/
├── CMakeLists.txt                 # основной проект
├── core/
│   ├── praytimes.cpp / .h         # C++ ядро (чистый алгоритм)
│   └── prayercalculator.h         # обёртка QObject для QML
├── qml/
│   ├── Main.qml                   # главный экран
│   ├── Settings.qml               # настройки
│   └── CompassView.qml            # компас
├── android/
│   ├── src/
│   │   └── com/example/namaz/     # Java-код для уведомлений
│   └── AndroidManifest.xml
├── resources/
│   ├── translations/              # .ts файлы
│   └── audio/                     # азан.mp3
└── main.cpp                       # точка входа
```

---

# 📅 Примерный график

| Этап | Срок | Результат |
|------|------|----------|
| Подготовка | 1–2 недели | Рабочая сборка Qt → Android |
| MVP (геолокация + времена) | 1.5 месяца | Приложение показывает намазы |
| Системные фичи (уведомления, компас) | 1.5 месяца | Полноценный помощник |
| Расширенный функционал | 2+ месяца | Рамадан, зикры, PDF и т.д. |
| **Итого** | **4–5 месяцев** | Готовое приложение в Google Play |

---

# 📌 Что делать прямо сейчас?

1. Установи **Qt 6.7+** с Android-компонентами.
2. Создай проект → собери "Hello World" на устройстве.
3. Напиши функцию `calculatePrayerTimes()` на C++.
4. Подключи её к QML.

Если хочешь — я могу:
- Прислать **готовое C++ ядро для расчёта намаза** (совместимое с Qt),
- Дать **шаблон QML-проекта с геолокацией**,
- Показать, **как вызвать Android-уведомление из Qt**.

Готов начать?