# Структура проекта Jummah Prayer

Проект разделен на **три независимые папки**:

## 📁 Структура проекта

```
jummah-prayer/
│
├── 📱 mobile/              # Мобильное приложение (Qt/QML)
│   ├── src/                # C++ логика (с Qt)
│   ├── qml/                # QML интерфейс
│   ├── CMakeLists.txt      # Сборка приложения
│   ├── macos/              # Настройки macOS
│   └── tests/              # Тесты
│
├── 🔧 backend/            # C++ Backend (HTTP сервер)
│   ├── src/
│   │   ├── server.cpp      # HTTP сервер
│   │   └── PrayerTimesCalculator.*  # Логика (без Qt)
│   └── CMakeLists.txt      # Сборка бэкенда
│
├── 🌐 frontend/           # Frontend (Web)
│   ├── index.html
│   ├── styles.css
│   ├── app.js
│   ├── prayer-calculator.js
│   ├── translations.js
│   └── manifest.json
│
├── Makefile               # Команды для всех компонентов
├── README.md              # Главный README
└── PROJECT_STRUCTURE.md   # Эта документация
```

---

## 📱 1. Мобильное приложение (mobile/)

**Директория:** `mobile/`

**Что это:**
- Нативное приложение для Desktop (macOS, Windows, Linux) и Android
- Использует Qt 6 с QML для интерфейса
- C++ логика в `mobile/src/`

**Файлы:**
```
mobile/
├── src/
│   ├── main.cpp                    # Точка входа
│   ├── PrayerTimesCalculator.*    # Расчет времени молитв
│   ├── LocationService.*          # Геолокация
│   ├── AppSettings.*              # Настройки
│   └── NotificationService.*      # Уведомления
├── qml/
│   ├── Main.qml                   # Главное окно
│   ├── MainPage.qml              # Страница времени молитв
│   ├── SettingsPage.qml          # Настройки
│   ├── DhikrPage.qml             # Зикр
│   └── components/               # UI компоненты
├── CMakeLists.txt
└── macos/Info.plist
```

**Сборка:**
```bash
make build          # Собрать приложение
make run            # Запустить приложение
```

**Использует:**
- Qt 6 (QML, Positioning, Sensors, Network)
- Ваша C++ логика расчета времени молитв

---

## 🔧 2. C++ Backend (backend/)

**Директория:** `backend/`

**Что это:**
- HTTP сервер на C++ (без Qt)
- Использует вашу логику расчета времени молитв
- Раздает статические файлы фронтенда
- Предоставляет REST API

**Файлы:**
```
backend/
├── src/
│   ├── server.cpp                    # HTTP сервер
│   ├── PrayerTimesCalculator.h       # Калькулятор (без Qt)
│   └── PrayerTimesCalculator.cpp     # Реализация
└── CMakeLists.txt                    # Сборка бэкенда
```

**Сборка:**
```bash
make web-backend-build    # Собрать бэкенд
make web-run              # Запустить бэкенд
```

**Использует:**
- cpp-httplib (header-only HTTP библиотека)
- Ваша логика расчета времени молитв (переписана без Qt)

**API:**
- `GET /api/prayer-times` - получить время молитв
- `POST /api/location` - установить местоположение
- Раздает статические файлы из `frontend/`

---

## 🌐 3. Frontend (frontend/)

**Директория:** `frontend/`

**Что это:**
- HTML/CSS/JavaScript интерфейс
- Работает в браузере
- Обращается к C++ бэкенду через API

**Файлы:**
```
frontend/
├── index.html              # Главная страница
├── styles.css              # Стили
├── app.js                  # Логика приложения
├── prayer-calculator.js    # Работа с API
├── translations.js         # Переводы
└── manifest.json           # PWA манифест
```

**Запуск:**
```bash
make web-start    # Запустить бэкенд + открыть фронтенд
```

**Использует:**
- Чистый JavaScript (без фреймворков)
- C++ бэкенд через REST API
- PWA поддержка

---

## 🔄 Общая логика

**Общая C++ логика расчета времени молитв:**
- `mobile/src/PrayerTimesCalculator.*` - используется в мобильном приложении
- `backend/src/PrayerTimesCalculator.*` - переписана без Qt для веб-версии

**Оба используют:**
- Один и тот же алгоритм расчета
- Те же методы расчета (MWL, ISNA, Egypt, Makkah, Karachi, Tehran)
- Те же мазхабы (Shafi'i, Hanafi)

---

## 📊 Сравнение компонентов

| Компонент | Технологии | Платформы | Зависимости |
|-----------|-----------|-----------|-------------|
| **Мобильное приложение** | Qt 6, QML, C++ | Desktop, Android | Qt 6 |
| **C++ Backend** | C++17, cpp-httplib | Любая (сервер) | Только стандартная библиотека |
| **Frontend** | HTML, CSS, JavaScript | Браузер | Нет (только браузер) |

---

## 🚀 Быстрый старт

### Мобильное приложение:
```bash
make build
make run
```

### Веб-версия:
```bash
make web-start    # Запускает бэкенд + фронтенд
```

### Только бэкенд:
```bash
make web-backend-build
make web-run
```

---

**Ассаламу алейкум!** 🌙
